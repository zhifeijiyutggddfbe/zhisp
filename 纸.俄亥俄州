if not game:IsLoaded() then
    game.Loaded:Wait()
end

local zhiUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/yda66722-hash/zhiUI/refs/heads/main/zhiUI"))()

local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ProximityPromptService = game:GetService("ProximityPromptService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character

if not Character then return end

local Humanoid = Character:WaitForChild("Humanoid", 5)
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart", 5)

local Options = {
    OnePunch = false,
    OneSwing = false,
    KillAura = false,
    StompAura = false,
    GrabAura = false,
    Killall = false,
    AutoEquip = false,
    Godmode = false,
    Invisible = false,
    RPGBomb = false,
    AutoArmor = false,
    Hitbox = false,
    ShowBuyUI = false,
    BlackMarket = false,
    Locker = false,
    FastInteract = false,
    ATMFarm = false,
    RegisterFarm = false,
    AutoRobBank = false,
    CashFarm = false,
    CashAura = false,
    ItemFarm = false,
    ItemAura = false,
    SItemsFarm = false,
    ReturnOnTeleport = false,
    NotifyAirdrop = false,
    ShowChat = false,
    Noclip = false,
    Fullbright = false,
    InfJump = false,
    HitboxSize = 15,
    HitboxTransparency = 0.8,
    WalkSpeed = 16,
    JumpPower = 50,
    SpamMessage = "纸.俄亥俄州 No.1",
}
local WalkSpeedMoved = false
local JumpPowerMoved = false
local SelectedItemFlags = { ["红卡"] = false, ["蓝卡"] = false, ["印钞机"] = false, ["气球"] = false }

local AirdropCallback = Instance.new("BindableFunction")

local RemotesModule = require(ReplicatedStorage.devv.client.Helpers.remotes.Signal)
local Remotes = debug.getupvalue(RemotesModule.FireServer, 1)
local ClientReplicator = require(ReplicatedStorage.devv.client.Helpers.objectProperties.ClientReplicator)
local Inventory = require(ReplicatedStorage.devv.client.Objects.v3item.modules.inventory)
local StateData = require(ReplicatedStorage.devv).load("state").data

local SelectedTarget = nil
local Whitelist = {
    2953444466,
    5509421902
}
local ItemsOnSaleList = {}
local Locations = {
    ["军械库"] = CFrame.new(671.68688964844, 6.2448601722717, -655.50268554688),
    ["银行"] = CFrame.new(1091.5296630859, 6.0434188842773, -457.62033081055),
    ["珠宝店"] = CFrame.new(1543.3168945312, 6.2433180809021, -682.63525390625),
    ["警察局"] = CFrame.new(655.10638427734, 9.035834312439, -903.20697021484),
    ["军事基地"] = CFrame.new(835.84875488281, 25.234800338745, -1327.0417480469),
    ["医院"] = CFrame.new(1112.4508056641, 6.0434203147888, -973.91772460938),
    ["游乐场"] = CFrame.new(1170.8796386719, 13.850684165955, -25.795112609863)
}

local function Notify(title, content, duration)
    zhiUI:Notify(tostring(title), tostring(content), duration or 3)
end

task.spawn(function()
    for _, item in pairs(workspace.ItemsOnSale:GetChildren()) do
        ItemsOnSaleList[item.Name] = item.Name
    end

    for _, conn in pairs(getconnections(game:GetService("RunService").Heartbeat)) do
        local fn = conn.Function
        if fn and getfenv(fn).script == ReplicatedStorage.devv.client.Handlers.ClientValidate then
            conn:Disable()
            Notify("纸.俄亥俄州：提示", "飞行/速度封禁绕过成功")
        end
    end

    getgenv().fixedremotes = {}
    for name, remote in next, RemotesModule do
        local upval = debug.getupvalue(remote, 1)
        if upval then
            getgenv().fixedremotes[name] = upval
        end
    end
end)

local function FindPlayer(input)
    local query = input:gsub("%s+", "")
    for _, player in pairs(Players:GetPlayers()) do
        if player.Name:lower():match("^" .. query:lower()) then
            return player
        end
        if player.DisplayName:lower():match("^" .. query:lower()) then
            return player
        end
    end
    Notify("纸.俄亥俄州：错误", "未找到玩家")
    return nil
end

local OriginalNamecall
OriginalNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local args = {...}
    local method = getnamecallmethod()

    if checkcaller() then
        return OriginalNamecall(self, ...)
    end

    if method == "FireServer" and self == Remotes.meleeItemHit then
        if tostring(args[1]) == "player" and not table.find(Whitelist, args[2].hitPlayerId) then
            if Options.OnePunch then
                if not string.find(tostring(args[2].meleeType), "swing") then
                    args[2].meleeType = "meleemegapunch"
                end
            end
            if Options.OneSwing then
                if string.find(tostring(args[2].meleeType), "swing") then
                    args[2].meleeType = "meleemegaswing"
                end
            end
            return OriginalNamecall(self, unpack(args))
        end
    end

    if Options.Hitbox and method == "FireServer" and self == Remotes.projectileHit then
        local hitPart = args[2].hitPart
        local model = hitPart:FindFirstAncestorOfClass("Model")
        local hitPlayer = Players:GetPlayerFromCharacter(model)
        if model and hitPlayer then
            args[2].hitPart = model.Hitbox["Head_Hitbox"]
            args[2].hitPlayerId = hitPlayer.UserId
            args[2].hitSize = args[2].hitPart.Size
            args[2].pos = args[2].hitPart.Position
        end
        return OriginalNamecall(self, unpack(args))
    end

    return OriginalNamecall(self, ...)
end)

workspace.Game.Entities.ItemPickup.DescendantAdded:Connect(function(obj)
    if Options.SItemsFarm and obj:IsA("ProximityPrompt") then
        local match = (SelectedItemFlags["红卡"] and obj.ObjectText == "Military Armory Keycard")
            or (SelectedItemFlags["蓝卡"] and obj.ObjectText == "Police Armory Keycard")
            or (SelectedItemFlags["印钞机"] and obj.ObjectText == "Money Printer")
            or (SelectedItemFlags["气球"] and obj.ObjectText:match("Balloon"))
        if match then
            local savedCF = HumanoidRootPart.CFrame
            Character:PivotTo(obj.Parent:GetPivot())
            task.wait()
            for i = 1, 5 do
                fireproximityprompt(obj)
                task.wait(0.1)
            end
            if Options.ReturnOnTeleport then
                Character:PivotTo(savedCF)
            end
        end
    end
end)

workspace.Game.Airdrops.ChildAdded:Connect(function(airdrop)
    if not Options.NotifyAirdrop then return end
    Notify("空投已刷新", "是否传送到空投？(3秒内点击)", 3)
    AirdropCallback.OnInvoke = function(response)
        if response == "是" then
            Character:PivotTo(airdrop:GetPivot())
        end
    end
end)

LocalPlayer.CharacterAdded:Connect(function(char)
    Character = char
    Humanoid = char:WaitForChild("Humanoid")
    HumanoidRootPart = char:WaitForChild("HumanoidRootPart")
    if Options.AutoEquip then
        Remotes.FireServer("equip", Inventory.getFromName("Fists").guid)
    end
end)

RunService.RenderStepped:Connect(function()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local char = player.Character
            if not char then continue end
            local hum = char:FindFirstChildOfClass("Humanoid")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hum or not hrp then continue end
            if char:FindFirstChild("ForceField") then continue end
            if table.find(Whitelist, player.UserId) then continue end

            if Options.KillAura then
                local dist = (HumanoidRootPart.Position - hrp.Position).Magnitude
                if dist < 35 and hum.Health > 5 then
                    Remotes.FireServer("meleeItemHit", "player", {
                        meleeType = "meleemegapunch",
                        hitPlayerId = player.UserId
                    })
                end
            end

            if Options.StompAura then
                if ClientReplicator.Get(player, "knocked") then
                    local dist = (HumanoidRootPart.Position - hrp.Position).Magnitude
                    if dist < 30 then
                        Remotes.FireServer("stomp", player)
                    end
                end
            end

            if Options.GrabAura then
                if ClientReplicator.Get(player, "knocked") then
                    local dist = (HumanoidRootPart.Position - hrp.Position).Magnitude
                    if dist < 35 then
                        Remotes.FireServer("grabPlayer", player)
                    end
                end
            end
        end
    end

    if Options.Godmode then
        if ClientReplicator.Get(LocalPlayer, "knocked") then
            ClientReplicator.Set(LocalPlayer, "knocked", false)
        end
    end

    if WalkSpeedMoved and Humanoid then
        Humanoid.WalkSpeed = Options.WalkSpeed
    end
    if JumpPowerMoved and Humanoid then
        Humanoid.JumpPower = Options.JumpPower
        Humanoid.UseJumpPower = true
    end

    if Options.Noclip and Character then
        for _, part in pairs(Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end

    if Options.Fullbright then
        Lighting.Ambient = Color3.new(1, 1, 1)
    else
        Lighting.Ambient = Color3.new(0, 0, 0)
    end
end)

UserInputService.JumpRequest:Connect(function()
    if Options.InfJump and Humanoid then
        Humanoid:ChangeState("Jumping")
    end
end)

local Window = zhiUI:CreateWindow({
    Title = "纸.俄亥俄州"
})

local TabCombat = Window:AddTab("战斗类")

TabCombat:AddToggle("一拳秒杀", false, function(v)
    Options.OnePunch = v
end)

TabCombat:AddToggle("其他近战武器秒杀", false, function(v)
    Options.OneSwing = v
end)

TabCombat:AddToggle("杀戮光环", false, function(v)
    Options.KillAura = v
    if v then
        Remotes.FireServer("equip", Inventory.getFromName("Fists").guid)
    end
end)

TabCombat:AddToggle("踩人光环", false, function(v)
    Options.StompAura = v
end)

TabCombat:AddToggle("抓人光环", false, function(v)
    Options.GrabAura = v
end)

TabCombat:AddToggle("杀死全部", false, function(v)
    Options.Killall = v
    if v then
        Remotes.FireServer("equip", Inventory.getFromName("Fists").guid)
    end
    HumanoidRootPart.Anchored = v
    while Options.Killall do
        task.wait()
        for _, player in pairs(Players:GetPlayers()) do
            if not Options.Killall then break end
            if player == LocalPlayer then continue end
            if table.find(Whitelist, player.UserId) then continue end
            local char = player.Character
            if not char then continue end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum then continue end
            if char:FindFirstChild("ForceField") then continue end
            if hum.Health <= 5 then continue end
            task.wait()
            Humanoid.Sit = false
            Character:PivotTo(char.HumanoidRootPart.CFrame)
            Remotes.FireServer("meleeItemHit", "player", {
                meleeType = "meleemegapunch",
                hitPlayerId = player.UserId
            })
            Remotes.FireServer("stomp", player)
            task.wait(0.7)
        end
    end
    HumanoidRootPart.Anchored = false
end)

TabCombat:AddToggle("死亡自动装备拳头", false, function(v)
    Options.AutoEquip = v
end)

TabCombat:AddToggle("防倒地", false, function(v)
    Options.Godmode = v
end)

TabCombat:AddToggle("隐身", false, function(v)
    Options.Invisible = v
    if v then
        local savedCF = HumanoidRootPart.CFrame
        Character:MoveTo(Vector3.new(-25.95, 84, 3537.55))
        task.wait(0.15)
        local chair = Instance.new("Seat", workspace)
        chair.Anchored = false
        chair.CanCollide = false
        chair.Name = "invischair"
        chair.Transparency = 1
        chair.Position = Vector3.new(-25.95, 84, 3537.55)
        local weld = Instance.new("Weld", chair)
        weld.Part0 = chair
        local torso = Character:FindFirstChild("Torso") or Character:FindFirstChild("UpperTorso")
        if torso then
            weld.Part1 = torso
            task.wait()
            Instance.new("Seat", workspace).CFrame = savedCF
        end
    else
        local chair = workspace:FindFirstChild("invischair")
        if chair then chair:Destroy() end
    end
end)

TabCombat:AddToggle("RPG全图轰炸", false, function(v)
    Options.RPGBomb = v
    if not v then return end
    if not Inventory.getFromName("RPG") then
        Remotes.InvokeServer("attemptPurchase", "RPG")
    end
    local equip = Inventory.getEquippedItem()
    if equip and equip.name ~= "RPG" then return end
    if equip then
        Remotes.FireServer("replicateProjectiles", equip.guid, {
            { "AmmoGuid", HumanoidRootPart.CFrame }
        }, "semi")
    end
    task.spawn(function()
        while Options.RPGBomb do
            task.wait()
            local item = Inventory.getEquippedItem()
            if not item or item.name ~= "RPG" then continue end
            for _, player in pairs(Players:GetPlayers()) do
                if not Options.RPGBomb then break end
                if player == LocalPlayer then continue end
                if table.find(Whitelist, player.UserId) then continue end
                local char = player.Character
                if not char then continue end
                local hum = char:FindFirstChildOfClass("Humanoid")
                if not hum or hum.Health <= 10 then continue end
                if char:FindFirstChild("ForceField") then continue end
                Remotes.FireServer("rocketHit", "AmmoGuid", "explosionGUID", char.HumanoidRootPart.Position)
            end
        end
    end)
    repeat task.wait(5) until not Options.RPGBomb
end)

TabCombat:AddToggle("自动穿甲", false, function(v)
    Options.AutoArmor = v
    if not v then return end
    task.spawn(function()
        while Options.AutoArmor do
            task.wait()
            if not Character then continue end
            local armor = LocalPlayer:GetAttribute("armor")
            if not armor or armor <= 0 then
                Remotes.InvokeServer("attemptPurchase", "Light Vest")
                local guid = Inventory.getFromName("Light Vest").guid
                Remotes.FireServer("equip", guid)
                Remotes.FireServer("useConsumable", guid)
                Remotes.FireServer("removeItem", guid)
            end
        end
    end)
end)

TabCombat:AddDivider()

local HitboxConnection
TabCombat:AddToggle("子弹范围 开关", false, function(v)
    Options.Hitbox = v
    if v then
        HitboxConnection = RunService.RenderStepped:Connect(function()
            for _, player in next, Players:GetPlayers() do
                pcall(function()
                    local sz = Options.HitboxSize
                    player.Character.HumanoidRootPart.Size = Vector3.new(sz, sz, sz)
                    player.Character.HumanoidRootPart.Transparency = Options.HitboxTransparency
                    player.Character.HumanoidRootPart.Color = Color3.fromRGB(0, 0, 0)
                    player.Character.HumanoidRootPart.Material = "Neon"
                    player.Character.HumanoidRootPart.CanCollide = false
                end)
            end
        end)
    else
        if HitboxConnection then HitboxConnection:Disconnect() end
        task.wait()
        for _, player in next, Players:GetPlayers() do
            pcall(function()
                player.Character.HumanoidRootPart.Size = Vector3.new(2, 2, 1)
                player.Character.HumanoidRootPart.Transparency = 1
                player.Character.HumanoidRootPart.Color = Color3.fromRGB(163, 162, 165)
                player.Character.HumanoidRootPart.BrickColor = BrickColor.new("Medium stone grey")
                player.Character.HumanoidRootPart.Material = "Plastic"
                player.Character.HumanoidRootPart.CanCollide = false
            end)
        end
    end
end)

TabCombat:AddInput("子弹范围大小", "默认 15", function(text)
    local n = tonumber(text)
    if n then Options.HitboxSize = n end
end)

TabCombat:AddInput("子弹范围透明度", "默认 0.8", function(text)
    local n = tonumber(text)
    if n then Options.HitboxTransparency = n end
end)

TabCombat:AddDivider()

TabCombat:AddInput("输入玩家名称", "输入名称后按回车", function(val)
    local found = FindPlayer(val)
    if found then
        SelectedTarget = found
        Notify("纸.俄亥俄州：白名单", "当前选择：" .. found.Name)
    else
        SelectedTarget = nil
        Notify("纸.俄亥俄州：白名单", "当前选择：无")
    end
end)

TabCombat:AddButton("添加至白名单", function()
    if not SelectedTarget then
        Notify("纸.俄亥俄州：错误", "请先输入名称")
        return
    end
    if not table.find(Whitelist, SelectedTarget.UserId) then
        table.insert(Whitelist, SelectedTarget.UserId)
        Notify("纸.俄亥俄州：白名单", "已添加 " .. SelectedTarget.Name)
    else
        Notify("纸.俄亥俄州：错误", "该玩家已经被添加至白名单")
    end
end)

TabCombat:AddButton("移除白名单", function()
    if not SelectedTarget then
        Notify("纸.俄亥俄州：错误", "请先输入名称")
        return
    end
    local idx = table.find(Whitelist, SelectedTarget.UserId)
    if idx then
        table.remove(Whitelist, idx)
        Notify("纸.俄亥俄州：白名单", "已移除 " .. SelectedTarget.Name)
    else
        Notify("纸.俄亥俄州：错误", "该玩家没有白名单，无法移除")
    end
end)

TabCombat:AddButton("清空白名单", function()
    table.clear(Whitelist)
    Notify("纸.俄亥俄州：白名单", "已清空")
end)

local TabItems = Window:AddTab("物品")
local SelectedItem = nil

local ItemNameList = {}
for k in pairs(ItemsOnSaleList) do
    table.insert(ItemNameList, k)
end
table.sort(ItemNameList)

TabItems:AddDropdown("选择物品", ItemNameList, function(val)
    SelectedItem = val
end)

TabItems:AddButton("购买", function()
    if SelectedItem then
        Remotes.InvokeServer("attemptPurchase", SelectedItem)
    else
        Notify("纸.俄亥俄州：错误", "请先选择物品")
    end
end)

TabItems:AddButton("购买子弹", function()
    if SelectedItem then
        Remotes.InvokeServer("attemptPurchaseAmmo", SelectedItem)
    else
        Notify("纸.俄亥俄州：错误", "请先选择物品")
    end
end)

TabItems:AddToggle("显示购买界面", false, function(v)
    Options.ShowBuyUI = v
    if not v then return end
    task.spawn(function()
        while Options.ShowBuyUI do
            task.wait()
            if not SelectedItem then continue end
            local itemNode = workspace.ItemsOnSale:FindFirstChild(SelectedItem)
            if itemNode then
                local td = itemNode:FindFirstChildOfClass("TouchDetector")
                if td then
                    firetouchinterest(td.Parent, HumanoidRootPart, 0)
                    firetouchinterest(td.Parent, HumanoidRootPart, 1)
                end
            end
        end
    end)
end)

TabItems:AddToggle("远程黑市", false, function(v)
    Options.BlackMarket = v
    workspace.BlackMarket.Dealer.Dealer.ProximityPrompt.MaxActivationDistance = v and 10000 or 20
end)

TabItems:AddToggle("远程储物柜", false, function(v)
    Options.Locker = v
    LocalPlayer.PlayerGui.Backpack.Holder.Locker.Visible = v
end)

local FastInteractConn
TabItems:AddToggle("快速互动", false, function(v)
    Options.FastInteract = v
    if v then
        FastInteractConn = ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt)
            fireproximityprompt(prompt)
        end)
    else
        if FastInteractConn then FastInteractConn:Disconnect() end
    end
end)

local TabAuto = Window:AddTab("自动")

TabAuto:AddToggle("自动打ATM", false, function(v)
    Options.ATMFarm = v
    if not v then return end
    if Character then
        Remotes.FireServer("equip", Inventory.getFromName("Fists").guid)
    end
    task.spawn(function()
        while Options.ATMFarm do
            task.wait()
            for _, atm in pairs(workspace.Game.Props.ATM:GetChildren()) do
                if not Options.ATMFarm then break end
                if atm:GetAttribute("state") ~= "destroyed" then
                    while atm:GetAttribute("state") ~= "destroyed" and Options.ATMFarm do
                        task.wait()
                        Character:PivotTo(atm:GetPivot())
                        Remotes.FireServer("meleeItemHit", "prop", {
                            meleeType = "meleepunch",
                            guid = atm:GetAttribute("guid")
                        })
                    end
                    task.wait(1)
                    for _, bundle in pairs(workspace.Game.Entities.CashBundle:GetChildren()) do
                        local cd = bundle:FindFirstChildOfClass("ClickDetector")
                        if cd and (HumanoidRootPart.Position - bundle:GetPivot().Position).Magnitude <= cd.MaxActivationDistance then
                            fireclickdetector(cd)
                            task.wait(0.5)
                        end
                    end
                end
            end
        end
    end)
end)

TabAuto:AddToggle("自动打收银机", false, function(v)
    Options.RegisterFarm = v
    if not v then return end
    if Character then
        Remotes.FireServer("equip", Inventory.getFromName("Fists").guid)
    end
    task.spawn(function()
        while Options.RegisterFarm do
            task.wait()
            for _, reg in pairs(workspace.Game.Props.CashRegister:GetChildren()) do
                if not Options.RegisterFarm then break end
                if reg:GetAttribute("state") ~= "destroyed" then
                    while reg:GetAttribute("state") ~= "destroyed" and Options.RegisterFarm do
                        task.wait()
                        Character:PivotTo(reg:GetPivot())
                        Remotes.FireServer("meleeItemHit", "prop", {
                            meleeType = "meleepunch",
                            guid = reg:GetAttribute("guid")
                        })
                    end
                    task.wait(1)
                    for _, bundle in pairs(workspace.Game.Entities.CashBundle:GetChildren()) do
                        local cd = bundle:FindFirstChildOfClass("ClickDetector")
                        if cd and (HumanoidRootPart.Position - bundle:GetPivot().Position).Magnitude <= cd.MaxActivationDistance then
                            fireclickdetector(cd)
                            task.wait(0.5)
                        end
                    end
                end
            end
        end
    end)
end)

TabAuto:AddToggle("自动抢银行", false, function(v)
    Options.AutoRobBank = v
    if not v then return end
    task.spawn(function()
        while Options.AutoRobBank do
            local cash = workspace.BankRobbery.BankCash.Cash
            repeat task.wait() until #cash:GetChildren() ~= 0
            pcall(function()
                HumanoidRootPart.CFrame = workspace.BankRobbery.VaultDoor.Door.CFrame
                fireproximityprompt(workspace.BankRobbery.VaultDoor.Door.Attachment.ProximityPrompt)
                task.wait(0.5)
            end)
            repeat task.wait() until not workspace.BankRobbery.VaultDoor.Door.Attachment.ProximityPrompt.Enabled
            HumanoidRootPart.CFrame = workspace.BankRobbery.BankCash.Pallet.CFrame
            fireproximityprompt(workspace.BankRobbery.BankCash.Main.Attachment.ProximityPrompt)
            task.wait(0.5)
            task.wait()
        end
    end)
end)

TabAuto:AddToggle("自动捡钱(未测试)", false, function(v)
    Options.CashFarm = v
    if not v then return end
    task.spawn(function()
        while Options.CashFarm do
            task.wait()
            for _, bundle in pairs(workspace.Game.Entities.CashBundle:GetChildren()) do
                if not Options.CashFarm then break end
                local cd = bundle:FindFirstChildOfClass("ClickDetector")
                if cd then
                    Character:PivotTo(bundle:GetPivot())
                    task.wait(0.5)
                    fireclickdetector(cd)
                    task.wait(1)
                end
            end
        end
    end)
end)

TabAuto:AddToggle("捡钱光环", false, function(v)
    Options.CashAura = v
    if not v then return end
    task.spawn(function()
        while Options.CashAura do
            for _, bundle in pairs(workspace.Game.Entities.CashBundle:GetChildren()) do
                local cd = bundle:FindFirstChildOfClass("ClickDetector")
                if cd and (HumanoidRootPart.Position - bundle:GetPivot().Position).Magnitude <= cd.MaxActivationDistance then
                    fireclickdetector(cd)
                end
            end
            task.wait(0.25)
        end
    end)
end)

TabAuto:AddToggle("自动捡物品", false, function(v)
    Options.ItemFarm = v
    if not v then return end
    task.spawn(function()
        while Options.ItemFarm do
            task.wait()
            for _, item in pairs(workspace.Game.Entities.ItemPickup:GetChildren()) do
                if not Options.ItemFarm then break end
                local cd = item:FindFirstChildWhichIsA("ClickDetector", true)
                if cd then
                    Character:PivotTo(cd.Parent.CFrame)
                    task.wait(0.5)
                    fireclickdetector(cd)
                    task.wait(1.5)
                end
            end
        end
    end)
end)

TabAuto:AddToggle("捡物品光环", false, function(v)
    Options.ItemAura = v
    if not v then return end
    task.spawn(function()
        while Options.ItemAura do
            for _, item in pairs(workspace.Game.Entities.ItemPickup:GetChildren()) do
                if not Options.ItemAura then
                    task.wait(0.25)
                    continue
                end
                pcall(function()
                    local cd = item:FindFirstChildWhichIsA("ClickDetector", true)
                    if cd and (HumanoidRootPart.Position - item:GetPivot().Position).Magnitude <= cd.MaxActivationDistance then
                        fireclickdetector(cd)
                    end
                end)
            end
        end
    end)
end)

TabAuto:AddDivider()

TabAuto:AddToggle("红卡", false, function(v) SelectedItemFlags["红卡"] = v end)
TabAuto:AddToggle("蓝卡", false, function(v) SelectedItemFlags["蓝卡"] = v end)
TabAuto:AddToggle("印钞机", false, function(v) SelectedItemFlags["印钞机"] = v end)
TabAuto:AddToggle("气球", false, function(v) SelectedItemFlags["气球"] = v end)

TabAuto:AddToggle("自动捡选中的物品", false, function(v)
    Options.SItemsFarm = v
    if not v then return end
    task.spawn(function()
        while Options.SItemsFarm do
            for _, item in pairs(workspace.Game.Entities.ItemPickup:GetDescendants()) do
                if not Options.SItemsFarm then break end
                if item:IsA("ProximityPrompt") then
                    local match = (SelectedItemFlags["红卡"] and item.ObjectText == "Military Armory Keycard")
                        or (SelectedItemFlags["蓝卡"] and item.ObjectText == "Police Armory Keycard")
                        or (SelectedItemFlags["印钞机"] and item.ObjectText == "Money Printer")
                        or (SelectedItemFlags["气球"] and item.ObjectText:match("Balloon"))
                    if match then
                        local savedCF = HumanoidRootPart.CFrame
                        Character:PivotTo(item.Parent:GetPivot())
                        task.wait()
                        for i = 1, 5 do
                            fireproximityprompt(item)
                            task.wait(0.1)
                        end
                        if Options.ReturnOnTeleport then
                            Character:PivotTo(savedCF)
                        end
                        task.wait(1.5)
                    end
                end
            end
            task.wait(0.5)
        end
    end)
end)

TabAuto:AddToggle("是否回传", false, function(v)
    Options.ReturnOnTeleport = v
end)

TabAuto:AddToggle("空投刷新提示", false, function(v)
    Options.NotifyAirdrop = v
end)

local TabTeleport = Window:AddTab("传送")
local SelectedLocation = nil

local LocationList = {}
for k in pairs(Locations) do
    table.insert(LocationList, k)
end

TabTeleport:AddDropdown("选择地点", LocationList, function(val)
    SelectedLocation = val
end)

TabTeleport:AddButton("传送", function()
    if SelectedLocation and Character then
        Character:PivotTo(Locations[SelectedLocation])
    end
end)

local TabFun = Window:AddTab("娱乐")
local FunTarget = nil

TabFun:AddInput("输入玩家名称", "输入名称后按回车", function(val)
    local found = FindPlayer(val)
    FunTarget = found
    if found then
        Notify("纸.俄亥俄州：娱乐", "当前选择：" .. found.Name)
    else
        Notify("纸.俄亥俄州：娱乐", "当前选择：无")
    end
end)

TabFun:AddInput("输入消息", "纸.俄亥俄州 No.1", function(val)
    if val and val ~= "" then
        Options.SpamMessage = val
    end
end)

TabFun:AddToggle("消息轰炸", false, function(v)
    Options.SpamPlayer = v
    if not v then return end
    if not FunTarget then
        Notify("纸.俄亥俄州：错误", "请先输入名称")
        Options.SpamPlayer = false
        return
    end
    task.spawn(function()
        while Options.SpamPlayer do
            task.wait(0.2)
            Remotes.FireServer("sendMessage", FunTarget.UserId, Options.SpamMessage)
        end
    end)
end)

TabFun:AddToggle("电话骚扰", false, function(v)
    Options.SpamCall = v
    if not v then return end
    if not FunTarget then
        Notify("纸.俄亥俄州：错误", "请先输入名称")
        Options.SpamCall = false
        return
    end
    task.spawn(function()
        while Options.SpamCall do
            task.wait(0.2)
            Remotes.InvokeServer("attemptCall", FunTarget.UserId)
        end
    end)
end)

TabFun:AddToggle("消息轰炸全体", false, function(v)
    Options.SpamAll = v
    if not v then return end
    task.spawn(function()
        while Options.SpamAll do
            task.wait(0.2)
            for _, player in pairs(Players:GetPlayers()) do
                if not Options.SpamAll then break end
                Remotes.FireServer("sendMessage", player.UserId, Options.SpamMessage)
            end
        end
    end)
end)

local TabData = Window:AddTab("数据")

local TabOther = Window:AddTab("其他")

TabOther:AddToggle("显示聊天框", false, function(v)
    Options.ShowChat = v
    game:GetService("TextChatService").ChatWindowConfiguration.Enabled = v
end)

TabOther:AddInput("设置物品栏数量", "输入数字", function(val)
    local n = tonumber(val)
    if n then Inventory.numSlots = n end
end)

TabOther:AddDivider()

TabOther:AddSlider("移动速度", 0, 500, 16, function(val)
    Options.WalkSpeed = val
    WalkSpeedMoved = true
end)

TabOther:AddSlider("跳跃高度", 0, 500, 50, function(val)
    Options.JumpPower = val
    JumpPowerMoved = true
end)

TabOther:AddButton("飞行", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Xingtaiduan/Script/main/Content/FlyGuiV3"))()
end)

TabOther:AddToggle("穿墙", false, function(v)
    Options.Noclip = v
    if not v and Humanoid then
        Humanoid:ChangeState("Flying")
    end
end)

TabOther:AddToggle("夜视", false, function(v)
    Options.Fullbright = v
    if not v then
        Lighting.Ambient = Color3.new(0, 0, 0)
    end
end)

TabOther:AddToggle("无限跳", false, function(v)
    Options.InfJump = v
end)

local TabSettings = Window:AddTab("设置")